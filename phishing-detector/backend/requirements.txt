# Core web framework
Flask==3.1.3
Werkzeug==3.1.3
itsdangerous==2.2.0
gunicorn==23.0.0


# Machine learning
scikit-learn==1.8.0
pandas==3.0.2
numpy>=1.26

# Optional, used opportunistically by ml/feature_extraction.py for live
# WHOIS lookups and redirect-chain following. The app degrades gracefully
# (simulated fallback values) if these aren't installed or the network
# call fails/times out, so they are optional rather than hard requirements.
# python-whois==0.9.4
# requests==2.32.3
